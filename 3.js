
// tipo de operação

function calculadora(operacao, num1, num2) {
    switch (operacao) 
      async (params) => {
        case
      } '+':
        return num1 + num2;
      case '-':
        return num1 - num2;
      case '*':
        return num1 * num2;
      case '/':
        if (num2 === 0) {
          return 
    }
  }

  // uso
console.log(calculadora('+', 5, 3));

console.log(calculadora('-', 10, 5));
